<style>
    .navbar-static-top{
        display: none;
    }
    .white-bg{
        display: none;
    }
    .navbar-static-side{
        display: none;
    }
    #page-wrapper{
        width: 100%;
    }
</style>
    <div class="middle-box text-center loginscreen animated fadeInDown">
        <div>
            <div>

                <h1 class="logo-name">IN+</h1>

            </div>
            <h3>로그인 페이지</h3>
            <a href="/">메인화면으로 돌아가기</a>
                <!--Continually expanded and constantly improved Inspinia Admin Them (IN+)-->
            </p>
            
            <form id="loginForm" class="m-t" role="form" action="/member/login" method="post">
                <div class="form-group">
                    <input type="text"id="userid" name="userid" class="form-control" placeholder="아이디(메일주소)"  onkeyup= 'printName()' />
                </div>
                <div class="form-group">
                    <input type="password" id="userpwd" name="userpwd"class="form-control" placeholder="암호">
                </div>
                <button type="submit" class="btn btn-primary block full-width m-b">로그인</button>

                
                <p class="text-muted text-center"><small>아이디가 없으신가요?</small></p>
                <a class="btn btn-sm btn-white btn-block" href="/member/entry">회원가입</a>
            </form>
            <p class="m-t"> <small>Languagewebsite &copy; 2022</small> </p>
        </div>
    </div>

 

<script>

    // 로그인 성공 시, 세션 스토리지에 유저 Id 저장
    var name;
    function printName()  {
        name = document.getElementById('userid').value;
        console.log("아이디 출력", name);
        sessionStorage.setItem("userId", name);
    }

    //패스포트 기반에서의 로그인 실패 데이터 수집(flash로 전달된 데이터)
    var loginError = '<%=loginError%>';

    //passport 플래시로 전달된 로그인 실패 메시지 출력...
    if(loginError.length > 0){
        alert(loginError);
    }


    //일반인증 방식의 로그인 결과 메시지값 출력하기
    var loginMsg = '<%=loginResult%>';

    //로그인 결과 메시지가 존재하면 에러내용을 alert출력한다.
    if(loginMsg.length > 0){
        alert(loginMsg);
    }


    

    $("#loginForm").submit(function(){
        //아이디/암호 입력여부 유효성 검사 실행
        return true;
    });



</script>


