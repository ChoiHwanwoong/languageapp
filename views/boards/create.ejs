          <!--우측 콘텐츠 헤더영역 -->
          <div class="row wrapper border-bottom white-bg page-heading">
            <div class="col-lg-10">
                <h2>게시글 등록</h2>
                <ol class="breadcrumb">
                    <li>
                        <a href="/">Home</a>
                    </li>
                    <li>
                        <a>   |   영어게시판 게시글   |   </a>
                    </li>
                    <li class="active">
                        <strong>게시글등록</strong>
                    </li>
                </ol>
            </div>
            <div class="col-lg-2">

            </div>
        </div>

        <!--우측 콘텐츠 영역 -->
        <div class="wrapper wrapper-content">

            <div class="ibox-content m-b-sm border-bottom">

                <form id="createform" action="/boards/create" method="post" enctype="multipart/form-data">

                    <div class="row">
                        <div class="col-sm-12">
                            <div class="form-group">
                                <label class="control-label">TEXTBOX</label>
                                <input type="text" name="title" id="title" class="form-control">
                            </div>
                        </div>
                    </div>
                    
                    <div class="row">
                        <div class="col-sm-12">
                            <div class="form-group">
                                <label class="control-label">TEXTAREA</label>
                                <textarea name="contents" id="contents" class="form-control" rows="10" cols="5"></textarea>
                            </div>
                        </div>
                    </div>

                    <input type="hidden" name="userId" id="userId" value="">
                 
                    
                    <div class="row">
                        <div class="col-sm-12">
                            <div class="form-group">
                                <label class="control-label">첨부파일</label>
                                <input type="file" name="file" id="file" class="form-control">
                            </div>
                        </div>
                    </div>

                    <div class="text-center">
                        <button type="submit" class="btn btn-primary">저장</button>
                        <a href="/boards/list" class="btn btn-info">목록</a>
                    </div>
                    
                </form>

            </div>
        </div>


        <script>

            //html요소가 웹브라우저에 모두 렌더링 완료되면 실행되는 메소드ready()
            $(document).ready(function(){
                console.log("페이지가 모두 로딩되었습니다.");
                document.getElementById('userId').setAttribute('value', sessionStorage.getItem('userId'));
            });

            function mySubmit () {

            }


            //서버로 전달하기전에 입력값 유효성 검사 처리하기  
            $("#createform").submit(function(){
                
                

                if($("#title").val() == ""){
                    alert("제목을 입력해주세요.");
                    $("#title").focus();
                    return false;
                }

                if($("#contents").val() == ""){
                    alert("내용을 입력해주세요.");
                    $("#contents").focus();
                    return false;
                }

                //서버로 입력 데이터를 전송합니다.
                return true;
            });

            // 추가
            // let user_id = document.createElement('input');
            // user_id.setAttribute('type', 'hidden');
            // user_id.setAttribute('name', 'userId');
            // user_id.setAttribute('id', 'userId');
            // user_id.setAttribute('value', sessionStorage.getItem('userId'));
            // $("#createform").app


        </script>
